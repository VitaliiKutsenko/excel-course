1)Webpack
        1.Webpack.config import/export
        2.Babel transpiler
        3.Scss
        4.EsLint
        5.Modes - dev (Dev server, SourceMap,EsLint,
                                Not Minified JS, css)
                - prod (minified code)

2)Git


if error {git commit -m "initial commit"
                  git push origin master}

git checkout -b "webpack" - ветка webpack

        -Production
            1.0.0 && 2.0.0
            ------
        -Development
            1.0.2 && 1.2.1
            _____________+___     PR(pool request)TEST
            \________________      |
